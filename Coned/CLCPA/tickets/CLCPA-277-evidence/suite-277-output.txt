======================================================================
CLCPA-277 -- the wrong year is named
  app.js : git show 7260063:Coned/CLCPA/ExecutiveDashboard_dev/app.js
  BASE   : 22c96cc (predates this change)
======================================================================

A. THE REPORTED CASE
  ok   A1 H1-2099-example.csv declares 2099
  ok   A2 importing it into 2097 raises an advisory
  ok   A3 naming BOTH years -- the file's and the target's
  ok   A4 and importing it into 2099 says nothing

B. DERIVED GENERICALLY, WITH DIGIT BOUNDARIES
  ok   B1 a table id in the name does not become a year -- A10-2025 gives 2025
  ok   B1b including ids whose digits look year-ish. MEASURED: a table id is a letter and one or two digits and cannot reach four, so the id-stripping step I first wrote changed the answer for NONE of 13 plausible names and was deleted rather than left as a branch that cannot fire
  ok   B2 a leading year is found
  ok   B3 and a 19xx year
  ok   B4 a name with no year declares none
  ok   B5 a five-digit run is NOT a year -- the digit boundary holds
  ok   B6 nor one with a digit in front
  ok   B7 a name carrying TWO different years declares neither
  ok   B8 and a browser's "(1)" suffix does not confuse it
  ok   B9 a year that is in no payload and no selector still resolves
  ok   B10 and the derivation names no year at all

C. BOTH SURFACES
  ok   C1 STAGED in the Add Data modal, from the dropdown the operator can change
  ok   C2 with its own staged line
  ok   C3 and POST-LOAD on the plan
  ok   C4 the post-load box is RED -- the CLCPA-266 component
  ok   C5 with its own heading
  ok   C6 and both years
  ok   C7 a file wrong in BOTH ways raises TWO red boxes -- different mistakes
  ok   C8 and a clean import raises neither

D. ADVISORY ONLY
  ok   D1 the advisory rejects nothing
  ok   D2 and the call site never touches plan.ok
  ok   D3 importIdentityNotice is BYTE-IDENTICAL to BASE
  ok   D4 and so is declaredTableFromFilename
  ok   D5 no added code line names a year
  ok   D6 nor a table

X. THE HARNESS
  ok   X1 BASE is a literal sha -- 22c96cc
  ok   X2 and predates this ticket
  ok   X3 the function under test is the one sliced from app.js

======================================================================
  32 passed, 0 failed
======================================================================
