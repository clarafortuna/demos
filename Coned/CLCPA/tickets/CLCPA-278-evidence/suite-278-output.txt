======================================================================
CLCPA-278 -- the engine's figure recomputes, the preparer's is kept
  app.js : c:\Users\emely\Desktop\Projects\demos\Coned\CLCPA\ExecutiveDashboard_dev\app.js
  BASE   : f2c7137 (predates this change)
======================================================================

A. WHOSE FIGURE IS IT
  ok   A1 a CONSISTENT total follows the edit: 1443 -> 1477 (got 1477)
  ok   A2 a total that ALREADY disagreed is KEPT: 1500 stays (got 1500)
  ok   A3 and CLCPA-272 advises on it, filed 1500 against 1477
  ok   A4 while the recomputed one raises NOTHING -- it is consistent again

B. THE EDGES
  ok   B1 editing the TOTAL ITSELF is the preparer filing one -- not overwritten
  ok   B2 a BLANK total is not consistent, so nothing is invented into it
  ok   B3 a row with a missing component is left alone -- a partial sum is not a sum
  ok   B3b and an edit that EMPTIES a component leaves the total alone (got 1443)
  ok   B4 a non-numeric total is left alone
  ok   B5 a total within the engine's own rounding tolerance counts as consistent
  ok   B6 editing the LABEL recomputes nothing

C. EVERY ENUMERATED DERIVABLE-COLUMN TABLE
  ok   C1 the rule works on all 7 enumerated tables -- 7

D. THE IMPORT PATH IS UNCHANGED
  ok   D1 a PROVIDED total still lands as filed
  ok   D2 and is still advised on
  ok   D3 an import leaving the column blank still succeeds
  ok   D4 buildIngestImport is BYTE-IDENTICAL to BASE

E. WHAT THIS MEANS FOR A "CHANGED CELL" (the CLCPA-269 interaction)
  ok   E1 one edit moved TWO cells: the component and its recomputed total
  ok   E2 column 1 (typed) and column 3 (derived)
     -> CLCPA-269 must count 1 here, not 2: the derived cell is the engine's, not the preparer's.

F. STYLE OF CHANGE
  ok   F1 the recompute takes the PRE-EDIT row
  ok   F2 and the blur handler captures it BEFORE the write
  ok   F3a all three steps of the blur handler are present
  ok   F3 they run in order: capture the row, write the cell, then recompute
  ok   F4 consistency uses the engine's own tolerance, not a new one
  ok   F5 the relationship comes from the schema
  ok   F6 no added code line names a table
  ok   F7 nor a column or a row
  ok   F8 no stored data: a save still writes rows only

X. THE HARNESS
  ok   X1 BASE is a literal sha -- f2c7137
  ok   X2 and predates this ticket
  ok   X3 the function under test is the one sliced from app.js
  ok   X4 the probe arithmetic checks out

======================================================================
  31 passed, 0 failed
======================================================================
