======================================================================
CLCPA-266 -- mutation controls
======================================================================
  GREEN THE DEFECT RETURNS: .ingest-import-notice loses its box again  -- NOT NOTICED. Not a guard.
  GREEN THE AMBER ACCENT is dropped, so the fraction notice looks like the info box  -- NOT NOTICED. Not a guard.
  GREEN THE RED ACCENT is dropped from the identity notice  -- NOT NOTICED. Not a guard.
  GREEN THE TWO ADVISORIES SHARE ONE ACCENT, so the amber says nothing  -- NOT NOTICED. Not a guard.
  GREEN AN ACCENT IS A LITERAL COLOUR instead of a token  -- NOT NOTICED. Not a guard.
  GREEN A VARIANT DECLARES MORE THAN THE ACCENT, becoming a second component  -- NOT NOTICED. Not a guard.
  GREEN THE TYPOGRAPHY does not follow the box into a notice  -- NOT NOTICED. Not a guard.
  GREEN THE BOXES TOUCH: the stacking margin is removed  -- NOT NOTICED. Not a guard.
  GREEN THE LAST CHILD keeps a trailing gap inside its box  -- NOT NOTICED. Not a guard.
  GREEN THE MODAL TWIN diverges: the staged warning goes back to amber text  -- NOT NOTICED. Not a guard.
  GREEN THE MODAL LAYOUT is changed alongside the warning  -- NOT NOTICED. Not a guard.
  red  STYLE ONLY BREAKS: a notice is reworded
       3 red, incl: L1 with the two class attributes undone, app.js is BYTE-IDENTICAL to BASE
  red  STYLE ONLY BREAKS: the two notices are reordered
       4 red, incl: S3 then the fraction notice
  red  STYLE ONLY BREAKS: the advisory starts rejecting
       2 red, incl: L1 with the two class attributes undone, app.js is BYTE-IDENTICAL to BASE
  red  THE ACCENT CLASS is put on the wrong notice
       4 red, incl: S3 then the fraction notice
  red  A CLEAN IMPORT starts rendering empty notice boxes
       2 red, incl: L7 a clean import still renders exactly ONE box
  red  HARNESS: the baseline is repointed at a symbolic ref
       9 red, incl: X5 BASE is a literal commit sha: HEAD
  red  HARNESS: the style claims stop resolving and just grep the text
       9 red, incl: B3 info keeps the blue it always had

  7 caught, 11 not caught, of 18 guards
  clean re-run against byte-restored source: PASSES -- 66 passed, 0 failed
