======================================================================
CLCPA-276 -- a notice lives as long as the draft it describes
  app.js : git show 22c96cc:Coned/CLCPA/ExecutiveDashboard_dev/app.js
  BASE   : 5d584c5 (predates this change)
======================================================================

A. WHILE THE DRAFT LIVES, the notices are on screen
  ok   A1 the import succeeded
  ok   A2 the receipt is rendered
  ok   A3 and the typed advisory
  ok   A4 and the reconciliation advisory, because Westchester does not add up

B. THE FOUR SHAPES FROM THE HOSTED BUILD
  ok   B1 shape 1: the receipt is GONE after Reset
  ok   B2 and so is the typed advisory
  ok   B3 and the reconciliation advisory, because the reset draft reconciles
  ok   B4 shape 3: the receipt is GONE after a successful save
  ok   B5 so "Nothing has been saved yet" cannot sit beside a recorded save
  ok   B6 but the reconciliation advisory RETURNS -- recomputed from the saved draft, which still does not add up. Cleared would have hidden a live fact
  ok   B7 shapes 2 and 4: after a year switch onto an empty year the mount is EMPTY -- no receipt, no advisory (got "")

C. ONE HELPER, EVERY EXIT
  ok   C1 the helper exists
  ok   C2 declared once and called from THREE places -- found 3 calls
  ok   C3 Reset clears
  ok   C4 a successful save clears
  ok   C5 and loading a table-year clears
  ok   C6 on BASE the clearing was inline and happened in ONE place only
  ok   C7 and no inline clear survives -- a fourth exit cannot half-clear

D. LIFECYCLE ONLY: the component and the texts are untouched
  ok   D1 renderIngestImportResult is BYTE-IDENTICAL to BASE
  ok   D1 renderTypedUnitNotice is BYTE-IDENTICAL to BASE
  ok   D1 renderReconcileNotice is BYTE-IDENTICAL to BASE
  ok   D2 no added code line touches the CLCPA-266 component
  ok   D3 nor any notice text
  ok   D4 the helper does NOT clear the reconciliation advisory -- it is recomputed from the draft, which is the "recomputed, not cleared" half

X. THE HARNESS
  ok   X1 BASE is a literal sha -- 5d584c5
  ok   X2 and predates this ticket
  ok   X3 the mount under test reacts to the state it is given

======================================================================
  27 passed, 0 failed
======================================================================
