======================================================================
CLCPA-274 -- the template marks derivable total columns (calculated)
  app.js : git show f2c7137:Coned/CLCPA/ExecutiveDashboard_dev/app.js
  BASE   : 050c1c1 (predates this change)
======================================================================

A. H1's GRAND TOTAL COLUMN, the shape the ticket names
  ok   A1 Grand Total is column 3
  ok   A2 on BASE every BODY row's Grand Total cell was unmarked -- blank and inviting
  ok   A3 they are now marked (calculated) -- 4 cells
  ok   A4 and the IMPORT accessor still says false for every one of them
  ok   A5 the total ROW was already marked and is unchanged

B. BEHAVIOUR UNCHANGED: a provided value is still accepted
  ok   B1 an import providing the Grand Total still succeeds
  ok   B2 and the provided 1500 LANDS -- not skipped as computed
  ok   B3 CLCPA-272 still reconciles it and advises
  ok   B4 no BODY row's Grand Total is reported as "not touched: computed"
  ok   B5 a file that LEAVES the marker imports its other cells
  ok   B6 and the marker is reported as left to the dashboard, not imported as text

C. THE ENUMERATED DERIVABLE-COLUMN TABLES
     B2    2 cells
     F2    2 cells
     F4    8 cells
     F5   39 cells
     F6   25 cells
     F7    3 cells
     H1    4 cells
  ok   C1 exactly the seven enumerated tables change -- got B2, F2, F4, F5, F6, F7, H1
  FAIL C2 and 81 cells in total -- got 83
  ok   C3 and they are exactly the tables detectSumColumns finds -- guidance and advisory cannot disagree about which columns are derivable

C2b. THE MARKER MUST NOT REACH A STRUCTURAL HEADER ROW
  ok   C2b1 F6 really does carry header_levels 2
  FAIL C2b2 its SECOND header row is NOT marked -- a header has nothing to calculate
  ok   C2b3 while the first body row below it IS
  ok   C2b4 and a body row whose figures are BLANK is still marked -- blanks are not text, and a fresh template is the case this ticket exists for
  FAIL C2b5 and F7's "% of Grand Total" row is not marked either -- its parts hold percentage strings, so it is not a sum of anything

D. STYLE OF CHANGE
  ok   D1 the template has its own accessor
  ok   D2 and the workbook writer calls it
  ok   D3 while the IMPORT still calls `any`
  ok   D4 on BASE the workbook writer called `any` -- one accessor for both
  ok   D5 the `any` accessor is BYTE-IDENTICAL to BASE
  ok   D6 no added code line names a table id
  ok   D7 nor a column
  ok   D8 the derivable columns come from the schema, not a list
  ok   D9 the group-header branch above it is unchanged and still checked FIRST
  ok   D10 and it is still checked BEFORE the calculated branch

X. THE HARNESS
  ok   X1 BASE is a literal sha -- 050c1c1
  ok   X2 and predates this ticket

======================================================================
  28 passed, 3 failed
======================================================================
