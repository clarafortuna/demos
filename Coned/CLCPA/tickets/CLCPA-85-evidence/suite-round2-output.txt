======================================================================
CLCPA-85 round 2 -- source tables row, the dialog, the removals
======================================================================

=== RULING 1: the two rows are IDENTICAL, at every table count ===
  ok   the tab markup exists in exactly ONE place in the source
  ok   exactly one definition of the shared renderer
  ok   and exactly two callers: the report pages and Report Data
  ok   compared every section at every active tab: 52 rows
  ok   CHARACTER FOR CHARACTER identical with only the action attribute normalised: 52/52
  ok   identical at 1 table (section I)
  ok   identical at 2 tables (section B)
  ok   identical at 4 tables (section D)
  ok   identical at 5 tables (section C)
  ok   identical at 9 tables (section J)
  ok   identical at 10 tables (section G)
  ok   the raw strings do differ, so the comparison is not vacuous
  ok   each row carries its own action attribute on all ten buttons

=== the report pages’ row did not change ===
  ok   section A: byte-identical to the pre-85 build
  ok   section B: byte-identical to the pre-85 build
  ok   section C: byte-identical to the pre-85 build
  ok   section D: byte-identical to the pre-85 build
  ok   section E: byte-identical to the pre-85 build
  ok   section F: byte-identical to the pre-85 build
  ok   section G: byte-identical to the pre-85 build
  ok   section H: byte-identical to the pre-85 build
  ok   section I: byte-identical to the pre-85 build
  ok   section J: byte-identical to the pre-85 build
  ok   BASE control: the row was built inline before, which is what moved
  ok   and the same literal is now inside the shared renderer
  ok   .src-tabs-row and .src-tab are byte-identical in CSS too, so nothing was restyled
  ok   the row is still repeat(10, 1fr), so few tables render exactly as the report pages render them

=== the picker row: table dropdown out, tab row in ===
  ok   the table dropdown is gone from the picker
  ok   and so are the options it built
  ok   the shared row is drawn with the Report Data action attribute
  ok   the divider label is present
  ok   and it sits OUTSIDE the row, above it
  ok   + Add year has left the picker row
  ok   the Remove year button stays, being a year action
  ok   BASE control: the dropdown existed
  ok   BASE control: so did + Add year
  ok   the picker is two columns now
  ok   BASE control: it was three, which is why it had to change
  ok   the tabs are wired by their own attribute
  ok   clicking the active tab is a no-op
  ok   and the dirty guard is kept
  ok   and it is no longer the browser-native one (CLCPA-230)
  ok   a tab click redraws the PAGE, so the active tab moves with it
  ok   and not just the editor, which would leave the old tab looking selected

=== round 3: ONE STEP, every control live ===
  ok   the Add New Year dialog exists
  ok   the page header button says Add Data, in title case
  ok   and it opens that dialog
  ok   the two-path dialog is gone by name
  ok   the choice state and Edit panel are gone
  ok   no choice buttons remain
  ok   nor their labels
  ok   and no comment still names the old dialog
  ok   the two-phase enable is gone
  ok   and any stage variable with it
  ok   nothing in the dialog is disabled any more
  ok   the choice CSS is deleted
  ok   and the gated-half CSS with it, not left orphaned
  ok   draw() was found to read
  ok   draw() writes no disabled attribute at all
  ok   the import bar is rendered
  ok   and NOT rendered inert
  ok   draw() renders dlg-newyear
  ok   draw() renders dlg-section
  ok   draw() renders dlg-table
  ok   with the existing-years hint
  ok   there is a staging wiring
  ok   which never touches the draft: staging is a read, not a write
  ok   the dry run plans against an EMPTY draft, which is what a new year has
  ok   and hands back the file name, its rows and the dry run
  ok   ONE engine does both the dry run and the apply, so they cannot disagree
  ok   there is a one-line summary
  ok   which says what is ready
  ok   or why it cannot be
  ok   and says plainly that a bad file does not block the year
  ok   the summary has a box in the dialog
  ok   styled, with a distinct look when the file cannot be imported
  ok   the resting background is transparent: transparent
  ok   there is a scoped hover rule for the control
  ok   and its hover background EQUALS its resting background: transparent
  ok   scoped by id, so no other control is affected
  ok   .btn:hover, .btn-link and .btn-link:hover are byte-identical to pre-85
  ok   so the underline on hover is the SIBLING rule s own, not one invented here
  ok   the template reads the typed year
  ok   generates the WORKBOOK for the selected table and typed year
  ok   with NO logo raster to await: round 6 removed it
  ok   and the handler is synchronous throughout
  ok   and names it -example.xlsx, since it is an example not a form
  ok   and the hint names the read-only example and the Save As step
  ok   with the workbook MIME type
  ok   the CSV template generator is gone
  ok   all three steps are present
  ok   validation comes FIRST, so an invalid year moves nothing
  ok   and the year is ADDED BEFORE the staged file is applied, always
  ok   an invalid year returns before anything is touched
  ok   nothing is imported before the add, even on the happy path
  ok   validation is its own function
  ok   and is side-effect free
  ok   and the commit uses it, so there is one set of rules
  ok   exactly ONE place adds a year
  ok   the redraw is marked
  ok   BEFORE the apply, so a rejected file cannot undo the year
  ok   only a clean plan is applied
  ok   but the result is recorded either way, so the page can say why
  ok   and closing redraws the page, so the year and the receipt both appear
  ok   the dialog has no year PICKER
  ok   buildIngestImport takes no year parameter
  ok   nor reads one from state
  ok   wireIngestImport is deleted: one step needs nothing sequenced from outside
  ok   and its rerender helper with it
  ok   BASE control: it never existed before this ticket either
  ok   still reuses the existing shell class ingest-modal-overlay
  ok   still reuses the existing shell class ingest-modal-head
  ok   still reuses the existing shell class ingest-modal-body
  ok   still reuses the existing shell class ingest-modal-foot
  ok   still reuses the existing shell class ingest-modal-close
  ok   the footer offers Cancel and the ONE primary action
  ok   and nothing else
  ok   Escape closes it
  ok   so does a backdrop click
  ok   and the Escape handler is removed on close

=== the old add-year modal is GONE, its mechanics kept ===
  ok   the dead modal is deleted
  ok   with its markup
  ok   BASE control: it existed
  ok   its mechanics survive as addReportingYear
  ok   same message as the deleted modal: "Please enter a valid year."
  ok   same message as the deleted modal: "Year must be between 2000 and 2100."
  ok   same message as the deleted modal: "already exists."
  ok   same persistence call
  ok   and it still refreshes the header year selector
  ok   exactly ONE place adds a year, so the two paths cannot diverge

=== ruling 4: the receipt is on the PAGE, the controls are not ===
  ok   the page renders the receipt and nothing else
  ok   the control bar is NOT on the page
  ok   and the receipt has its mount, above the editor
  ok   both controls are in the bar, which only the dialog renders
  ok   the bar has one definition and one caller, the fill stage
  ok   the page no longer wires the controls it does not render

=== ruling 5: Export PDF is gone from every page ===
  ok   the button markup is gone
  ok   and its label
  ok   the wiring function is gone
  ok   and its boot call
  ok   nothing calls window.print any more
  ok   BASE control: button and wiring both existed
  ok   the @media print block is KEPT
  ok   still hiding the sidebar
  ok   still setting page margins
  ok   and byte-identical to before, so printing is unchanged

=== ruling 2: Reporting Year hidden on Report Data ONLY ===
  ok   there is a per-route sync
  ok   keyed on the Report Data route
  ok   hiding BOTH the label and the select
  ok   and it runs on every route change
  ok   the select stays in the DOM
  ok   and so does its label
  ok   nothing is removed from the DOM
  ok   buildYearSelector still finds it, untouched
  ok   because state.year has many readers: 44

=== round 3: the ONE STEP, EXECUTED against a DOM stub ===
  ok   one dialog, titled Add Data
  ok   the year input is there
  ok   defaulting to the page year, 2025, rather than the next unused one
  ok   the next-unused suggestion is gone
  ok   with the existing-years hint
  ok   Section and Table, present
  ok   and both import controls
  ok   NOTHING is disabled: every control is live
  ok   and nothing is greyed
  ok   with no staged file yet
  ok   the footer offers Cancel and Add Year
  ok   and one primary action, not two
  ok   the template button rasterises NOTHING: the logo step is gone
  ok   it builds the workbook for the selected table straight away
  ok   and no logo reaches the builder at all
  ok   called with exactly TWO arguments, counted at the call itself
  ok   and with the TYPED year 2026, not the page year 2025
  ok   the page year never reaches the builder
  ok   and the file is NAMED for the typed year, with the workbook MIME type
  ok   and the bytes it returns are handed straight to the download
  ok   the page stays on its own table while the dialog shows another
  ok   choosing a file runs the dry run
  ok   and does NOT apply it
  ok   the draft is untouched by staging
  ok   and no year was added
  ok   the staged box appears
  ok   naming the file
  ok   with a one-line summary of what it holds
  ok   and not flagged bad
  ok   Add Year added the year
  ok   and applied the staged file
  ok   IN THAT ORDER: the add precedes the apply
  ok   the page is on the new year
  ok   and moved to the table the DIALOG chose, not the one it was on: A1
  ok   the draft holds the imported rows: 2
  ok   the result is recorded for the page panel
  ok   the dialog closed
  ok   and the page redrew after it
  ok   changing the dialog Section does NOT move the page: cancel is free
  ok   the template button builds one workbook: 1
  ok   for the table the DIALOG is on, not the page s A1: buildIngestWorkbook:B1:1999:args=2
  ok   which is a section B table: B1
  ok   and downloading a template still moves nothing on the page
  ok   an invalid year adds nothing
  ok   imports nothing
  ok   and does not close the dialog
  ok   the page year is untouched
  ok   and so are its section and table: an invalid year moves NOTHING
  ok   with the error shown on the field: Year must be between 2000 and 2100.
  ok   an existing year adds nothing either
  ok   and no longer errors about it: loading an existing year is the point
  ok   the year is added
  ok   nothing is imported
  ok   and no result panel is set
  ok   the dialog closes
  ok   and the page redraws with the new year
  ok   a hard-rejecting file is flagged bad at staging
  ok   saying it cannot be imported
  ok   and why
  ok   and that the year will still be added
  ok   Add Year STILL adds the year
  ok   and the page moves to it
  ok   nothing is applied
  ok   the failure is recorded for the page panel
  ok   with its reasons: This cell holds a formula rather than a value. Save the file
  ok   and the dialog closes

renders regenerated from the shipped code:
   1-picker-source-tables.html
   1b-picker-few-tables.html
   2-dialog-one-step.html
   3-dialog-file-staged.html
   4-dialog-file-rejected.html
   5-topbar.html

======================================================================
  221 passed, 0 failed
======================================================================
